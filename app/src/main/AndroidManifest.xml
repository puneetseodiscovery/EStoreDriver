<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.estoreid.estoredriver">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dashboard.Congrats_Screen"></activity>
        <activity android:name=".dashboard.otp.OTP_Screen" />
        <activity android:name=".dashboard.delivered.DeliveredDetailPage" />
        <activity android:name=".dashboard.accept.Accept" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/googleclientId" />

        <activity android:name=".login.LoginScreen" />
        <activity android:name=".dashboard.MainActivity" />
        <activity android:name=".splash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>